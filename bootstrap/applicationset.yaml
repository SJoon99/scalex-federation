apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: scalex-federation-releases
  namespace: argo
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - git:
        repoURL: https://github.com/SJoon99/scalex-federation.git
        revision: experiment/release-per-directory
        files:
          - path: releases/*/*/release.yaml
      selector:
        matchLabels:
          state: active
  template:
    metadata:
      name: 'federation-{{ .environment }}-{{ .name }}'
      namespace: argo
      labels:
        app.kubernetes.io/part-of: scalex-federation
        scalex.io/environment: '{{ .environment }}'
        scalex.io/release: '{{ .name }}'
    spec:
      project: scalex-federation
      sources:
        - repoURL: '{{ .source.repoURL }}'
          targetRevision: '{{ .source.revision }}'
          path: '{{ .source.path }}'
          helm:
            releaseName: '{{ .name }}'
            valueFiles:
              - '$federation/{{ .values.path }}'
        - repoURL: https://github.com/SJoon99/scalex-federation.git
          targetRevision: experiment/release-per-directory
          ref: federation
      destination:
        name: karmada
        namespace: '{{ .namespace }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
          - ServerSideApply=true
          - SkipDryRunOnMissingResource=true
